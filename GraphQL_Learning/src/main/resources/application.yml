server:
  port: 9095

  http2:
    enabled: true
spring:
  datasource:
    url: jdbc:h2:mem:graphqllll
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto:update
    show-sql: true
  graphql:
    graphiql:
      enabled: true
